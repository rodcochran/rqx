worker_processes auto;

events {
    worker_connections 4096;
}

http {
    include /etc/nginx/mime.types;

    server {
        listen 80;
        location /json {
            default_type application/json;
            alias /usr/share/nginx/html/response.json;
        }
    }
}