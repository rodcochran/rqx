node_modules/
bin/
*.tsbuildinfo

# Per-stack configs carry your SSH allowed CIDR (your IP) and AWS profile, so
# they stay local. Pulumi.yaml (project + shared defaults) and the .example
# template are committed.
Pulumi.*.yaml
!Pulumi.yaml

# Local copy of bench results — large, and mirrored to S3 by bench.sh.
results/

.known_hosts.*
results/.current-run
