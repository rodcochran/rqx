node_modules/
bin/
*.tsbuildinfo

# Don't commit per-stack configs by default — they contain your SSH allowed CIDR
# (which discloses your IP) and any other operator-specific values. Commit
# selectively if you'd rather share a fixed stack with collaborators.
Pulumi.*.yaml
!Pulumi.yaml

# Local mirror of S3 bench results — these can get large and they're
# already preserved in S3.
results/
